<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define log file location -->
    <property name="LOG_DIR" value="logs" />
    <property name="LOG_FILE" value="playbot" />

    <!-- Console appender for development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <!-- Single rolling file appender for all logs -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_FILE}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Roll over daily and when file reaches 10MB -->
            <fileNamePattern>${LOG_DIR}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <!-- Keep 7 days of logs -->
            <maxHistory>7</maxHistory>
            <!-- Total size cap of 100MB -->
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Reduce JDA verbosity -->
    <logger name="net.dv8tion.jda" level="INFO" />
    <logger name="net.dv8tion.jda.internal" level="WARN" />

    <!-- Reduce Spring verbosity in production -->
    <logger name="org.springframework" level="INFO" />
    <logger name="org.springframework.security" level="INFO" />
    
    <!-- Application logging -->
    <logger name="com.discordbot" level="INFO" />

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>
